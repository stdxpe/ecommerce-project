import 'package:flutter/cupertino.dart';

class DialogPopupWidget extends StatelessWidget {
  const DialogPopupWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
